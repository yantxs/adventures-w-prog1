#!/usr/bin/env python
# -*- coding: utf-8 -*-
# EXERCICIO PROPOSTO
# Yan Teixeira da Silva - 20172BSI0149
#  teste.py

#ABERTURA
def main():
	tempopad1, tempopad1, tempopad2, tempopad3=0.0,0.0,0.0,0.0
	inscricao=0
	tempo, tempo1, tempo2, tempo3=0.0,0.0,0.0,0.0;pts=0.0;total=0.0
	campeao=0;maior=0;etapa=0
	#FEEDING
	tempopad1=float(input("Insira o tempo da primeira etapa\n"))
	tempopad2=float(input("Insira o tempo da primeira etapa\n"))
	tempopad1=float(input("Insira o tempo da primeira etapa\n"))
	inscricao=int(input("Numero de inscrição da equipe\n"))
	#FEEDINGTEAMS
	while(inscricao != 9999):
		if(inscricao != 9999):
			for i in range(3):
					i=i+1
					print("Etapa Nº", i)
					tempo=float(input("Entre com o tempo gasto pela equipe nesta etapa\n"))
					if (etapa == i):
						tempo1=tempo
					elif(etapa == i):
						tempo2=tempo
					elif(etapa ==i):
						tempo3=tempo
		D=tempo		
						#FLAG
			inscricao=int(input("Numero de inscrição da equipe\n"))
			#PROGRAM	
		if(etapa1 < 3)
			ponto1 = 100;
			else if(etapa1 >= 3 && etapa1 <= 5)
			ponto1 = 80;
			else
			ponto1 = 80 - ((etapa1 - 5)/5);
		if(etapa2 < 3)
			ponto2 = 100;
			else if(etapa2 >= 3 && etapa2 <= 5)
			ponto2 = 80;
			else
			ponto2 = 80 - ((etapa2 - 5)/5);
		if(etapa3 < 3)
			ponto3 = 100;
			else if(etapa3 >= 3 && etapa3 <= 5)
			ponto3 = 80;
			else
			ponto3 = 80 - ((etapa3 - 5)/5);
			total = ponto1 + ponto2 + ponto3;
		if(total > vencedor){
			vencedor = total; team = insc;
		}

if __name__ == '__main__':
	main()
X'
